# Stage 2.1 — проверка 24 сентября 2026

## Автоматические тесты — PASS

- logic.test.mjs: 10 078 проверок исходной колоды и карточек, Mixed, неверного/двойного клика, таймеров, auto/manual перехода, score и нормализации настроек.
- rotation.test.mjs: 20 000 детерминированных раундов с seeded random. Наборы 25/40, все размеры 3–10, 1–4 карточки в допустимых режимах; четыре полных цикла целей. Каждая цель ровно один раз за цикл, на границе нет повторения, каждый concept появляется и становится целью. Отвлекающие элементы не голодают, разброс счётчиков не больше 2. Любые две карточки пересекаются ровно по цели. Mixed всегда image ↔ word. Недостаточные размеры отклоняются.
- motion.test.mjs: 518 400 кадров: 3–10 элементов, word/image/mixed, Slow/Medium/Fast (+25%). Нет пересечений AABB или выхода из круга, включая большой временной шаг после паузы. Каждый элемент удаляется от начальной позиции минимум на 0,337 диаметра — постоянного застревания не обнаружено.
- content.test.mjs: размеры наборов, неверные URL/данные/IDs/imageSource, JSON schema, недоступное хранилище, wrong duration 0,54 с и amplitude +35%.

## Браузер — PASS

Среда: Codex In-app Browser, http://127.0.0.1:4173.

Реальный Ctrl+V (через буфер браузера, не синтетический paste):
- Выбран общий Upload / Paste images, заполнен Word.
- Текст cat вставляется в Word как текст.
- Текст в upload-области показывает No image found in clipboard, не попадает в поля.
- PNG blob 80×40 вставлен Ctrl+V: preview → Save → reload → PNG остаётся → Dobble показывает изображение с naturalWidth 80/naturalHeight 40.
- В URL-режиме HTTPS-ссылка вставлена Ctrl+V без преобразования; preview загрузился (544 px), после Save/reload URL и источник сохранены.

Другой ввод:
- Реальный Browse / Replace открывает file chooser, PNG загружается и заменяет preview.
- editor-inputs.html: синтетические браузерные Drop/Paste с настоящим File, Remove, повторная вставка, сохранение Blob и source, свежее открытие редактора — все проверки PASS. Перетаскивание из проводника мышью отдельно не выполнялось.
- Подтверждение смены источника: Cancel сохраняет старый источник; Change меняет поля, но сохраняет прежнюю картинку до замены. Сохранение такого набора и новые emoji-строки проверены.
- Unsaved changes: Leave/Cancel в приложении работает.

Игра:
- Новый набор с 40 уникальными животными открыт через ?set=ID.
- 4 карточки × 10 элементов, Fast: отображается 40 движущихся DOM-элементов.
- Mixed переключает count с 4 на 2, общий concept имеет image и word.
- Неверный ответ не меняет score; правильный даёт +1 и feedback. Ручной режим ждёт New cards; auto-next создаёт новые карточки. Reset score работает.
- Theme Ocean применяется, Sound/Auto next переключаются, Fullscreen входит/выходит.
- Маленький набор из 3: невозможная конфигурация показывает нужное число концептов; 1 карточка × 3 разрешена. Недопустимый запуск в Settings заблокирован.
- Номера раунда и фиксированного поднабора 13 в UI нет.

Геометрия:
- flexible.html: 36 комбинаций, по 45 кадров реальной DOM-геометрии: все размеры 3–10 в Images/Words/Mixed на Fast, дополнительно Slow/Medium на 3 и 10. От 2 до 4 карточек. Все AABB без пересечений и внутри кругов. Измерения используются только тестом, а не игровым animation loop.
- layout.html: 15 проверок Hub/My Sets/нового и заполненного Editor/игры на 390, 768, 1280 px, без горизонтального переполнения. Для игры создаётся временный набор из 40, чтобы текущая настройка 10/card была допустима.

Хранилище:
- browser.html: PNG/JPEG/WEBP → upload → save → fresh read → decode, edit, independent duplicate/new IDs, перенос blob в JSON и обратно, imageSource после reload/export/import, invalid JSON, защита builtin, выборочное delete, corrupted record warning — PASS.
- Delete через UI: Cancel сохраняет, подтверждение удаляет только созданную тестовую копию. Существующие пользовательские наборы не удалялись.
- В конце проверены console error/warn у игры, редактора, хранилища, input и layout тестов: пусто.

## Найдено и исправлено

- Извлечение цели из bag теперь выполняется один раз на карточный раунд, а не при каждом сравнении ID.
- При добавлении строки во время обработки upload обновляется актуальный preview; версии загрузки предотвращают перезапись более нового изображения старой операцией.
- Подсказка о сохранённом предыдущем изображении исчезает после его замены/удаления.

## Границы проверки

Полное покрытие целей/отвлекающих элементов проверено детерминированно в тестах; вручную не проигрывались все 40 целей. Воспринимаемая громкость через ClassIn и колонки ученика не оценивалась. Wrong усилен на 35% по спецификации; окончательный слуховой баланс требует teacher test-drive. Тесты выполнялись в одном браузерном окружении.

В My Sets оставлены Stage 2.1 — 40 concepts (40 разных животных), Stage 2.1 Paste Test и Stage 2.1 URL Test для повторной проверки. Автоматические временные наборы удалены тестами. Ваши прежние наборы сохранены.
